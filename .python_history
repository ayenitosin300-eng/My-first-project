import package_name   # imports the package
print(package_name.__version__)  # prints its version
import package_name
# See what’s inside the module
print(dir(package_name))
import package_name
# Call the function
package_name.foo()
# For the current session
export PATH=$HOME/local/bin:$PATH
exit
import tkinter as tk
from tkinter import messagebox
sudo apt install tk-dev
sudo apt install libncurses5-dev libncursesw5-dev  # sometimes also needed
sudo apt install tk-dev
sudo apt install libncurses5-dev libncursesw5-dev  # sometimes also needed
sudo apt install tk-dev
sudo apt install libncurses5-dev libncursesw5-dev  # sometimes also needed
sudo apt install tk-dev
sudo apt install libncurses5-dev libncursesw5-dev  # sometimes also needed
sudo apt install tk-dev
import package_name
print(dir(package_name))  # see functions/classes
package_name.foo()        # call your function
python3.12 -m tkinter
